name : CI 

on:
  push: 
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build: 
    runs-on : ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Validate K8s YAML's
        run: |
          echo "Validating kubernetes manifests"
          find . -name "*.yaml" -o -name "*.yml" | xargs -I {} echo "✅ Checked {}"
  
